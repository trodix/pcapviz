# Build artifacts
/bin/
web/node_modules/
web/dist/

# Captures (don't commit sample/test pcaps)
*.pcap
*.pcapng
*.cap

# NOTE: the built frontend in internal/adapter/http/dist/ IS committed so that a
# plain `go build` (without Node) produces a working binary thanks to //go:embed.
# Rebuild it with `make web`.

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# CI release output
/out/
